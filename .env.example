# Copy this file to .env and fill in your API keys.
# Leave OPENAI_API_KEY empty (or remove the line) to use web-search-only mode.
OPENAI_API_KEY=
TAVILY_API_KEY=

# Whisper: auto | cuda | cpu  (auto = use GPU if CUDA is available)
WHISPER_DEVICE=auto
WHISPER_MODEL=base
